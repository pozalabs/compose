[tool.poetry]
name = "fastapi-example"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pozalabs-compose = {extras = ["fastapi", "logging"], version = "^0.42.0"}
uvicorn = {extras = ["fastapi", "logging"], version = "^0.30.1"}
pydantic-settings = "^2.3.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
